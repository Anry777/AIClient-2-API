# Визуальная консоль управления AIClient2API

## Обзор

AIClient2API теперь включает полнофункциональную визуальную Web UI-консоль управления, которая позволяет через браузер удобно управлять конфигурацией, мониторить состояние пула провайдеров, смотреть логи в реальном времени, настраивать провайдеров AI-моделей и т.д.

## Возможности

### 🎨 Современный интерфейс
- Адаптивный дизайн, поддержка десктопа и мобильных устройств
- Наглядный дашборд со статистикой системы
- Боковая навигация для быстрого переключения модулей
- Переключение вкладок протокола (протокол OpenAI / протокол Claude)

### 📊 Мониторинг в реальном времени
- **Дашборд**: аптайм, информация о системе, версия Node.js, время на сервере, использование памяти
- **Управление пулом провайдеров**: статус аккаунтов, статистика использования, процент ошибок
- **Статистика активности**: активные соединения, активные провайдеры, количество здоровых провайдеров

### ⚙️ Управление конфигурацией
- Онлайн-изменение API-ключа, адреса прослушивания и порта
- Поддержка нескольких провайдеров моделей:
  - **Gemini CLI OAuth** - доступ к Gemini с обходом ограничений
  - **OpenAI Custom** - пользовательская конфигурация OpenAI API
  - **Claude Custom** - пользовательская конфигурация Claude API
  - **Claude Kiro OAuth** - сервис Claude с обходом ограничений / бесплатным использованием
  - **Qwen OAuth** - OAuth-аутентификация Tongyi Qianwen
  - **OpenAI Responses** - новая версия OpenAI API
- Редактирование системного промпта
- Расширенные параметры конфигурации:
  - Путь к файлу системного промпта и режим
  - Настройка логирования промптов
  - Механизм повторных попыток (макс. число ретраев, базовая задержка)
  - Настройки автообновления OAuth-токена
  - Путь к файлу конфигурации пула провайдеров

### 🔧 Управление загруженными конфигами
- Поиск конфигурационных файлов
- Фильтрация по статусу привязки (привязано/не привязано)
- Отображение списка конфигов
- Статистика по конфигам (всего/привязано/не привязано)
- Обновление списка конфигов в реальном времени

### 🛣️ Примеры вызовов через маршрутизацию по пути
- **Мгновенное переключение**: смена провайдера AI-модели через изменение пути в URL
- **Кросс-протокольные вызовы**: поддержка вызова моделей Claude через протокол OpenAI, или вызова OpenAI через протокол Claude
- **Подсказки по настройке клиентов**: примеры конфигурации для Cherry-Studio, NextChat, Cline и т.д.
- Полные примеры использования `curl`
- Функция «копировать путь эндпоинта» в один клик

Примеры поддерживаемых маршрутов:
- `/gemini-cli-oauth/v1/chat/completions` - Gemini CLI OAuth (протокол OpenAI)
- `/gemini-cli-oauth/v1/messages` - Gemini CLI OAuth (протокол Claude)
- `/openai-qwen-oauth/v1/chat/completions` - Qwen OAuth (протокол OpenAI)
- `/openai-qwen-oauth/v1/messages` - Qwen OAuth (протокол Claude)
- `/claude-custom/v1/chat/completions` - Claude Custom (протокол OpenAI)
- `/claude-custom/v1/messages` - Claude Custom (протокол Claude)
- `/claude-kiro-oauth/v1/chat/completions` - Claude Kiro OAuth (протокол OpenAI)
- `/claude-kiro-oauth/v1/messages` - Claude Kiro OAuth (протокол Claude)
- `/openai-custom/v1/chat/completions` - OpenAI Custom (протокол OpenAI)
- `/openai-custom/v1/messages` - OpenAI Custom (протокол Claude)

### 📜 Логи в реальном времени
- Отображение логов сервера в реальном времени
- Очистка логов
- Переключение авто-скролла и ручной прокрутки
- Управление буфером логов

### 🔔 Система уведомлений
- Уведомления об успехе/ошибке операций
- Аккуратные Toast-уведомления
- Автоматическое скрытие через 3 секунды

## Быстрый старт

### Запуск сервера

При запуске сервера браузер автоматически откроется на странице консоли управления:

```bash
node src/api-server.js --port 3001 --api-key 123456
```

Адрес: http://127.0.0.1:3001/

### Запуск через Docker Compose

Если ты используешь Docker, можно запустить сервис через `docker compose`:

```bash
docker compose up -d --build
```

Адрес: http://127.0.0.1:3001/

### Навигация по интерфейсу

1. **Дашборд** - обзор системы, статистика и примеры маршрутизации по пути
2. **Управление конфигурацией** - изменение параметров сервера и настроек провайдеров
3. **Управление пулом провайдеров** - управление несколькими аккаунтами/API-провайдерами
4. **Управление загруженными конфигами** - управление конфигами, поиск и фильтрация
5. **Логи в реальном времени** - просмотр логов работы сервера

## API-эндпоинты

Консоль управления использует следующие RESTful API-эндпоинты:

### Управление конфигурацией
- `GET /api/config` - получить текущую конфигурацию
- `POST /api/config` - обновить конфигурацию
- `GET /api/configs` - получить список конфигурационных файлов
- `POST /api/configs/search` - поиск конфигурационных файлов

### Системная информация
- `GET /api/system` - получить информацию о системе
- `GET /api/providers` - получить информацию о пуле провайдеров

### Данные в реальном времени
- `GET /api/events` - поток Server-Sent Events для обновлений в реальном времени

### Статические файлы
- `GET /` - главная страница
- `GET /index.html` - HTML-страница
- `GET /app/styles.css` - стили
- `GET /app/mobile.css` - стили для мобильных
- `GET /app/app.js` - основная логика JavaScript
- `GET /app/utils.js` - утилиты
- `GET /app/config-manager.js` - управление конфигурацией
- `GET /app/provider-manager.js` - управление провайдерами
- `GET /app/event-stream.js` - обработка event-stream
- `GET /app/event-handlers.js` - обработчики событий
- `GET /app/navigation.js` - логика навигации
- `GET /app/modal.js` - компонент модального окна
- `GET /app/file-upload.js` - загрузка файлов
- `GET /app/upload-config-manager.js` - управление загрузкой конфигов
- `GET /app/routing-examples.js` - примеры маршрутов
- `GET /app/constants.js` - константы

## Техническая реализация

### Фронтенд-стек
- **HTML5** - семантическая разметка и поддержка доступности
- **CSS3** - современная стилизация (CSS Variables, Flexbox, Grid)
- **JavaScript (ES6+)** - модульная логика взаимодействий
- **Server-Sent Events** - доставка данных в реальном времени
- **Font Awesome 6.4.0** - библиотека иконок

### Интеграция с бэкендом
- Интегрировано в существующий `api-server.js`
- Ноль дополнительных зависимостей
- Синтаксис ES-модулей
- Асинхронная обработка

### Коммуникация в реальном времени
Server-Sent Events используется для реализации связи в реальном времени:
- Автоматическая трансляция логов всем подключенным клиентам
- Отправка обновлений статистики каждые 5 секунд
- Лёгкая реализация без WebSocket

## Структура файлов

```
static/
├── index.html              # Главная страница
└── app/
    ├── styles.css          # Основной файл стилей
    ├── mobile.css          # Стили для мобильных
    ├── app.js              # Основная логика приложения
    ├── constants.js        # Константы
    ├── utils.js            # Утилиты
    ├── config-manager.js   # Управление конфигурацией
    ├── provider-manager.js # Управление провайдерами
    ├── upload-config-manager.js # Управление загрузкой конфигов
    ├── event-stream.js     # Обработка event-stream
    ├── event-handlers.js   # Обработчики событий
    ├── navigation.js       # Логика навигации
    ├── modal.js           # Компонент модального окна
    ├── file-upload.js     # Загрузка файлов
    └── routing-examples.js # Примеры маршрутов
```

## Поддерживаемые провайдеры

### Типы с обходом ограничений
- **Gemini CLI OAuth** - обход ограничений Gemini API через OAuth
- **Claude Kiro OAuth** - бесплатный сервис Claude
- **Qwen OAuth** - OAuth-аутентификация Tongyi Qianwen

### Официальные API / сторонние
- **OpenAI Custom** - пользовательский endpoint OpenAI API
- **Claude Custom** - пользовательский endpoint Claude API
- **OpenAI Responses** - актуальная версия OpenAI API

## Совместимость с браузерами

- Chrome 60+
- Firefox 55+
- Safari 11+
- Edge 79+

## Часто задаваемые вопросы

### Q: После запуска сервера браузер не открылся автоматически?
A: Проверь, что сервер запущен с `localhost` или `127.0.0.1`. Автооткрытие включено только при локальном развертывании.

### Q: Как пользоваться маршрутизацией по пути?
A: На дашборде в разделе "Примеры вызовов через маршрутизацию по пути" посмотри примеры для разных провайдеров. Достаточно изменить URL API-эндпоинта в клиенте, чтобы переключить AI-модель.

### Q: Как настроить OAuth-провайдеров?
A: На странице управления конфигурацией выбери нужного OAuth-провайдера и укажи Project ID и OAuth-учётные данные (поддерживается путь к файлу или Base64).

### Q: Для чего нужна загрузка конфигурационных файлов?
A: Загрузка конфигов позволяет отправить локальные конфигурационные файлы на сервер, чтобы удобнее управлять ими и синхронизировать конфигурацию между окружениями.

### Q: Как посмотреть более подробную информацию о провайдерах?
A: На странице "Пул провайдеров" можно увидеть подробности по каждому провайдеру: количество вызовов, количество ошибок, время последнего использования и т.д.

### Q: Нужно ли перезапускать сервер после изменения конфигурации?
A: Большинство параметров (например, системный промпт или API-ключ) применяется сразу, но изменения сетевых параметров (например, порт) требуют перезапуска сервера.

### Q: Какие клиенты поддерживаются?
A: Поддерживаются популярные AI-клиенты (Cherry-Studio, NextChat, Cline и др.) — достаточно указать API endpoint на соответствующий маршрут.

## Руководство по использованию маршрутов

### 1. Выбор провайдера
Выбери путь провайдера в зависимости от того, к какой AI-модели хочешь обращаться.

### 2. Выбор формата протокола
- **Протокол OpenAI**: использовать endpoint `/v1/chat/completions`
- **Протокол Claude**: использовать endpoint `/v1/messages`

### 3. Настройка клиента
В AI-клиенте укажи:
- **API endpoint**: `http://localhost:3001/{путь провайдера}/{путь протокола}`
- **API key**: ключ соответствующего провайдера
- **Имя модели**: конкретную модель, поддерживаемую провайдером

### 4. Отправка запросов
Отправляй запросы в формате выбранного протокола — это позволяет делать кросс-протокольные вызовы.

## Вклад

Приветствуются Issue и Pull Request, улучшающие эту консоль управления!

## Лицензия

Этот модуль использует ту же лицензию, что и основной проект.
